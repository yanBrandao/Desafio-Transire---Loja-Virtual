@model IEnumerable<DesafioTransire.Models.Products>
@{
    ViewBag.Title = "Home Page";
}



<div class="jumbotron">
    <h1>Oscar vídeo Locadora</h1>
    <p class="lead">
        A Oscar vídeo locadora, está com uma proposta para revolucionar o mercado do cinema.
        <br />
        Conosco, você poderá alugar seus filmes sem sair de casa, realizamos entrega do maior acervo de manaus em sua casa!
        <br />
        Mais de 2.000 exemplares com as melhores fitas cassetes até os mais recentes Blu-rays.
    </p>

    <p>@Html.ActionLink("Conheça nosso acervo!", "Index", "Products", new { @area = "" }, new { @class = "btn btn-primary btn-lg" })</p>

</div>

<div class="row">
    <div class="col-md-4">
        <h2>
            Produtos em destaque (Lançamentos)
        </h2>
    </div>
</div>
<div class="row">

    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <form id="form1" runat="server">
                <div>

                    <h3>@Html.DisplayFor(modelItem => item.Name)</h3>
                    <img src="@Html.DisplayFor(modelItem => item.URL)" width="300px" height="411px" />
                    <p>
                        @Html.DisplayFor(modelItem => item.Description)
                    </p>
                    @Html.ActionLink("Ver mais", "../Products/Details", new { id = item.ID }, new { @class = "btn btn-secondary" })
                   
                </div>
            </form>
        </div>
    }
   
                    
    
</div>
